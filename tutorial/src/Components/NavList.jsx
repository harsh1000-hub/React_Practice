import React from 'react'

function NavList({data,darkMode,popUpMode}) {
  return (
    <div>
     <div>NavList</div>
     <button onClick={popUpMode}>Try it Bro</button>
    </div>
   
  )
}

export default NavList