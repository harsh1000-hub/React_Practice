import React from 'react'

function Navlist({data,metaData,popMode}) {
  return (
    <div>
    <div>Navlist</div>
    <button onClick={popMode}>Try it bro</button>
    </div>
  )
}

export default Navlist